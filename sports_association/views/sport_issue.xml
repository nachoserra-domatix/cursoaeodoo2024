<odoo>
    <!-- model.name action window -->
    <record id="action_sport_issue" model="ir.actions.act_window">
        <field name="name">sport.issue.action</field>
        <field name="res_model">sport.issue</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="domain">[]</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Create a new sport issue
            </p>
        </field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, that's why It needs NO parent or action -->
    <menuitem id="sport_menu" name="Sport Issues" sequence="10"/>
    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="sport_issue_menu" name="Sport Issues" parent="sport_menu" action="action_sport_issue" sequence="10"/>
</odoo>